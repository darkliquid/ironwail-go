# Interface

## Main consumers

- backend-specific render paths and renderer helper code that need shared asset-side behavior

## Contracts

- this node provides shared renderer asset helpers that concrete backends can consume without redefining asset bookkeeping each time
