package {{.Pkg}}

// QuakeGo gameplay sources live here. They are compiled to progs.dat by
// cmd/qgo (see mise run build-progs) and also build as plain Go for
// qcmod test / In-Go simulation runs — keep samples compatible with both.