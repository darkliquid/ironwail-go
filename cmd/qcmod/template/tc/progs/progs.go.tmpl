package {{.Pkg}}

// QuakeGo gameplay sources live here (compiled to progs.dat by cmd/qgo; also
// plain Go for qcmod test / sim runs).
//
// A total conversion usually ports the full gameplay surface. The canonical
// QuakeGo reference implementation is pkg/qgo/quakego in the engine repo —
// copy it into this directory and adapt classname handlers to your game.